{
  "name": "TwitchListOverlay",
  "version": "1.0.0",
  "description": "Used to display a simple list overlay in OBS or whatever",
  "main": "main.js",
  "scripts": {
    "build": "npx babel src --out-dir bin --presets react-app/prod",
    "watch": "npx babel --watch src --out-dir bin --presets react-app/prod"
  },
  "keywords": [],
  "author": "github.com/matthewjohnston4",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-react-app": "^3.1.2"
  }
}
